{
  "name": "commonform-analyze",
  "description": "analyze the relationships within a Common Form form",
  "version": "1.0.0-rc1",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (http://kemitchell.com)",
  "dependencies": {
    "array-find": "^0.1.1",
    "commonform-predicate": "^0.4.0"
  },
  "devDependencies": {
    "chai": "^2.2.0",
    "check-versions": "^1.0.0",
    "istanbul": "^0.3.5",
    "jscs": "^1.8.1",
    "jshint": "^2.5.10",
    "mocha": "^2.0.1"
  },
  "homepage": "https://github.com/commonform/commonform-analyze",
  "keywords": [
    "contracts",
    "document assembly",
    "knowledge management",
    "law",
    "markup"
  ],
  "license": "Apache-2.0",
  "main": "commonform-analyze.js",
  "repository": "commonform/commonform-analyze",
  "scripts": {
    "check": "check-versions --check-module",
    "coverage": "istanbul cover _mocha && istanbul check-coverage coverage/coverage.json",
    "lint": "jshint commonform-analyze.js test && jscs commonform-analyze.js test",
    "pre-commit": "npm run lint && npm run check && npm run coverage",
    "test": "mocha",
    "travis-ci": "npm run pre-commit"
  }
}
